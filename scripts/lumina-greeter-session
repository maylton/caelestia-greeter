#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [[ -x "$script_dir/caelestia-greeter-session" ]]; then
    exec "$script_dir/caelestia-greeter-session" "$@"
fi

exec /usr/local/bin/caelestia-greeter-session "$@"
